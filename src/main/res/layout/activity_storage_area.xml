<?xml version="1.0" encoding="utf-8"?>
<!--
    Storage Area Screen Layout

    Purpose:
      - Displays all items for a specific storage area (Fridge, Freezer, Cupboard).
      - Used by StorageAreaActivity.
      - Provides:
          * A header with an Add Item button
          * A RecyclerView for item listing
          * A modal overlay for adding/editing items

    Architectural Role:
      - Pure UI layout; contains no logic.
      - Works with:
          * FoodItemAdapter (RecyclerView rendering)
          * InputParser + FormValidator (validation)
          * ExpiryService (urgency logic)
          * StorageAreaActivity (overlay behaviour)
      - Demonstrates clean separation of concerns.

    UX Notes:
      - Overlay behaves like a modal dialog but implemented in XML for flexibility.
      - Header bar provides a consistent Add Item entry point.
      - RecyclerView supports large lists efficiently.
-->
<FrameLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="?android:attr/selectableItemBackground">

    <!-- MAIN CONTENT AREA -->
    <LinearLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="40dp">

        <!-- Header with Add Item button -->
        <LinearLayout
                android:id="@+id/addItemHeader"
                android:layout_width="match_parent"
                android:layout_height="65dp"
                android:background="@color/green"
                android:gravity="center_vertical"
                android:paddingTop="10dp"
                android:paddingBottom="10dp"
                android:paddingStart="50dp"
                android:paddingEnd="16dp"
                android:orientation="horizontal">

            <TextView
                    android:id="@+id/addItemTitle"
                    android:layout_width="20dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="@string/add_item"
                    android:textColor="@android:color/white"
                    android:textSize="20sp"
                    android:gravity="center" />

            <ImageView
                    android:id="@+id/addItemButton"
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:background="@drawable/circle_background"
                    android:padding="8dp"
                    android:src="@drawable/baseline_add_24"
                    app:tint="@android:color/white" />
        </LinearLayout>

        <!-- Storage Area Title -->
        <TextView
                android:id="@+id/areaTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/area"
                android:textSize="24sp"
                android:layout_marginTop="16dp" />

        <!-- RecyclerView for item list -->
        <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/itemsRecyclerView"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:padding="8dp"
                android:clipToPadding="false"
                android:scrollbars="vertical" />
    </LinearLayout>

    <!-- MODAL OVERLAY FOR ADD / EDIT ITEM -->
    <ScrollView
            android:id="@+id/addItemOverlay"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:background="@android:color/white"
            android:elevation="120dp"
            android:visibility="gone"
            android:padding="16dp">

        <LinearLayout
                android:orientation="vertical"
                android:layout_width="match_parent"
                android:layout_height="wrap_content">

            <!-- Item name -->
            <EditText
                    android:id="@+id/inputItemName"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:hint="@string/item_name"
                    android:inputType="text" />

            <!-- Expiry date -->
            <EditText
                    android:id="@+id/inputExpiryDate"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:hint="@string/expiry_date"
                    android:focusable="true"
                    android:clickable="true"
                    android:layout_marginTop="8dp" />

            <!-- Use-by type spinner -->
            <Spinner
                    android:id="@+id/inputUseByType"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp" />

            <!-- Quantity -->
            <EditText
                    android:id="@+id/inputQuantity"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:hint="@string/quantity"
                    android:inputType="number"
                    android:layout_marginTop="8dp" />

            <!-- Price -->
            <EditText
                    android:id="@+id/inputItemPrice"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:hint="@string/price"
                    android:inputType="numberDecimal"
                    android:layout_marginTop="8dp" />

            <!-- Notes -->
            <EditText
                    android:id="@+id/inputNotes"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:hint="@string/notes_optional"
                    android:inputType="textMultiLine"
                    android:minLines="2"
                    android:layout_marginTop="8dp" />

            <!-- Save + Cancel buttons -->
            <LinearLayout
                    android:orientation="horizontal"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:gravity="center"
                    android:layout_marginTop="12dp">

                <Button
                        android:id="@+id/saveItemButton"
                        android:text="@string/save"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginEnd="12dp" />

                <Button
                        android:id="@+id/cancelOverlayButton"
                        android:text="@string/cancel"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content" />
            </LinearLayout>

            <!-- Remove section (visible only in Edit mode) -->
            <LinearLayout
                    android:id="@+id/removeSection"
                    android:orientation="horizontal"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:gravity="center"
                    android:visibility="gone"
                    android:layout_marginTop="12dp">

                <Spinner
                        android:id="@+id/removeReasonSpinner"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content" />

                <Button
                        android:id="@+id/removeButton"
                        android:text="@string/remove"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="12dp" />
            </LinearLayout>

        </LinearLayout>
    </ScrollView>

</FrameLayout>
